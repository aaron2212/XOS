TODO:
    - Fix the timer and write "time.h"
    - Fix shift key press glitch in kbd.c?
    - Fix characters not being printed properly when typing?
    - Fix shell memory allocation failure problem
    - Create open files table and check if the file being opened is already in the table
    - (change printf() to kprintf())

FUTURE TODO:
    - add putc() ability to write to different streams
