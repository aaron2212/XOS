#include "syscalls.h"

/*
    Func: sys_getpid
    Get the process ID (uid) for the currently running user

    Returns:
        The process ID for the currently running user
*/

int sys_getpid()
{
    return -1;
}